# OpenRouter Configuration (REQUIRED)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model (optional, defaults to anthropic/claude-sonnet-4)
# See available models: https://openrouter.ai/models
OPENROUTER_MODEL=anthropic/claude-sonnet-4

# Optional: Your site info for OpenRouter rankings
OPENROUTER_SITE_URL=
OPENROUTER_SITE_NAME=Apollo Agent

# GitHub API (optional, for enhanced repo search)
GITHUB_TOKEN=

# Session Storage
SESSION_DIR=.sessions

# ========================================
# Sudo Configuration
# ========================================

# Sudo Mode (choose one):
# - 'auto': Automatic detection (default)
# - 'interactive': Always prompt for password (secure, manual)
# - 'password': Use SUDO_PASSWORD from env (convenience, less secure)
# - 'nopasswd': Assume passwordless sudo is configured (recommended for production)
SUDO_MODE=auto

# Sudo Password (only used if SUDO_MODE=password)
# ⚠️ WARNING: Storing passwords in plaintext is a security risk!
# ⚠️ Only use this for personal development environments with disk encryption
# ⚠️ Better alternatives:
#   1. Configure passwordless sudo (see docs/SUDO-SETUP.md) - RECOMMENDED
#   2. Use interactive mode (SUDO_MODE=interactive)
# SUDO_PASSWORD=your_sudo_password_here

# Note: MCP Servers are launched automatically
# - Serena: uvx --from git+https://github.com/oraios/serena serena start-mcp-server
# - Sequential Thinking: npx @modelcontextprotocol/server-sequential-thinking
#
# Requirements:
# - uv (for Serena): https://docs.astral.sh/uv/getting-started/installation/
# - Node.js >= 18 (for Sequential Thinking)

